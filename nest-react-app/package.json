{
  "name": "nest-react-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "heroku-postbuild": "NODE_ENV=dev cd ./apps/client && npm install && cd ../server && npm install && cd ../.. && npm install --omit --no-shrinkwrap && npm run build",
    "start": "node apps/server/dist/src/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
