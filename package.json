{
  "name": "nest-react-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "heroku-postbuild": "NODE_ENV=dev npm install && cd ./apps/client && npm install && cd ../server && npm install && prisma generate",
    "start": "node apps/server/dist/src/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "1.6.3"
  }
}
